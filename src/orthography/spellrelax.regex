# Russian

X (->) Х, # roman numerals written in cyrillic
#C (->) С, # --"-- Relaxing the C results in massive over analysis with the preposition с
M (->) М, # --"--
x (->) х, # --"--
#c (->) с, # --"--
m (->) м  # --"--
;

# Explanation:
# lexical side (->) input 
# Or, in other words:
# correct (->) in use out there
